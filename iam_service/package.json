{
  "name": "iam_service",
  "scripts": {
    "watch": "cargo watch -- npm-run-all watch:*",
    "watch:rsbuid": "npm run build:rsbuild",
    "watch:server": "cargo run",
    "build": "npm-run-all build:*",
    "build:rsbuild": "rsbuild build",
    "build:cargo-build": "cargo build"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.0.1-beta.3",
    "@tailwindcss/forms": "^0.5.7",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.6"
  },
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "htmx.org": "^2.0.1"
  }
}
